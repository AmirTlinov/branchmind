[package]
name = "bm_mcp"
version = "0.1.0"
edition = "2024"

[dependencies]
bm_core = { path = "../core" }
bm_storage = { path = "../storage" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
time = { version = "0.3", features = ["formatting"] }
